name        = "alignment_header_column"
kind        = "regression"
description = "Header row plus 3 data rows, one row inserted, aligned on the first column with RowKey { columns: vec![1] }. Through 2.6.0 it covered AlignmentMode::HeaderColumn, the same alignment under another name, removed in 3.0.0 (RFC-036 #4)."
notes       = ""
